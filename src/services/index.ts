import { getResolverFunc, linkPhoneToWalletFunc } from './resolver';
import {
  getRecordFunc,
  isRecordVerifiedFunc,
  recordExistsFunc,
  setPhoneRecordFunc,
} from './record';

export {
  getResolverFunc,
  linkPhoneToWalletFunc,
  getRecordFunc,
  isRecordVerifiedFunc,
  recordExistsFunc,
  setPhoneRecordFunc,
};
